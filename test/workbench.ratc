
struct Vec2 {
	float x;
	float y;

	method null translate(float a, float b){
		this.x = this.x + a;
		this.y = this.y + b;
	}
}

// Entry-point. Should return true if errors occured
fn bool program(){
	int t = 1 + 4;
	int g = 2  + testFunc(t);

	Vec2 aha;
	aha.translate(0.5f, 0.1f);


	return false;
}

fn int testFunc(int g){
	return g + 1;
}